server:
  port: 7001

spring:
  application:
      name: eureka-server

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/
    # 是否需要进行服务注册
    register-with-eureka: false
    # 是否需要进行服务拉取
    fetch-registry: false